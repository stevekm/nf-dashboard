<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Nextflow Dashboard</title>
</head>

<body>
    <div>
        <h1>Nextflow Dashboard</h1>
        <h2>Workflow: <%= runid %></h2>
        <p>Messages:</p>
        <div>
          <!-- loop over messages and render them -->
          <% messages.forEach((message) => { %>
            <a href="/message/<%= message.id %>" class="list-group-item">
              <h4><%= message.id %> : <%= message.body.runStatus %></h4>
            </a>
          <% }) %>
        </div>
    </div>
</body>

</html>
